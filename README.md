# Weather Parsing Python 🌤

Парсер данных о погоде с сайта world-weather.ru. Получает прогноз погоды на неделю для Санкт-Петербурга и представляет данные в формате JSON.

## Описание

Проект представляет собой веб-скрапер, который извлекает данные о погоде с сайта world-weather.ru. Программа собирает информацию о температуре, ощущаемой температуре, вероятности осадков, давлении, влажности и параметрах ветра для каждого временного периода (ночь, утро, день, вечер).

## Возможности

- Получение прогноза погоды на 7 дней
- Детализация по 4 периодам суток (ночь, утро, день, вечер)
- Сбор полных метеорологических данных:
  - Температура и ощущаемая температура
  - Вероятность осадков
  - Атмосферное давление
  - Влажность воздуха
  - Направление и скорость ветра
- Вывод данных в структурированном JSON формате
- Обработка ошибок и валидация данных

## Быстрый старт

### Требования

- Python 3.10+
- pip

### Установка

1. Клонируйте репозиторий:
```bash
git clone https://github.com/your-username/weather_parsing_python.git
cd weather_parsing_python
```
2. Установите зависимости:
```bash
pip install -r requirements.txt
```
3. Запустите парсер:
```bash
python main.py
```

## Зависимости
- requests>=2.31.0 — HTTP-запросы
- beautifulsoup4>=4.12.0 — парсинг HTML

## Использование
### Базовый пример
```python
from weather_parsing_python import Parser
import json

# Создание парсера
parser = Parser("https://world-weather.ru/pogoda/russia/saint_petersburg/")

# Получение данных
weather_data = parser.parse()

# Вывод результата
if weather_data:
    print(weather_data)
```
### Сохранение в файл

```python
weather_data = parser.parse()

if weather_data:
    with open("weather.json", "w", encoding="utf-8") as f:
        f.write(weather_data)
    print("Данные сохранены в weather.json")
```
## Структура выходных данных
```json
{
  "01.03.2026": {
    "night": {
      "temp": "+1°",
      "fells_like": "-1°",
      "probability": "21%",
      "pressure": "763",
      "wind": {
        "direction": "западный",
        "speed": "1.6"
      },
      "humidity": "87%"
    },
    "morning": {...},
    "day": {...},
    "evening": {...}
  }
}
```
### Примечание:
Проект предназначен для образовательных целей. При использовании парсера соблюдайте правила целевого сайта и не отправляйте слишком частые запросы.
